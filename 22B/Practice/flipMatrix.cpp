/*
1. flipMatrix that takes a matrix (2-dim array) and flips it around the 
	middle vertical line that separates the left from the right.
2. printMatirx that takes a matrix (2-dim array) and prints its contents.
Test the flip function using a matrix defined in the main function. 
First print the matrix, then flip it and print the result, then flip it again and print the result.
*/
